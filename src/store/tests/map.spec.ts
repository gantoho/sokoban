import { it, test, expect, describe } from 'vitest'

describe('map', () => {
  it('should first', () => {
    expect(true).toBe(true)
  })
  test('should first', () => {
    expect('first').toBe('first')
  })
})
